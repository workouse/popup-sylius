workouse_popup_plugin_admin:
  resource: "@WorkousePopupPlugin/Resources/config/admin_routing.yml"
  prefix: /admin

workouse_popup_plugin_shop:
  resource: "@WorkousePopupPlugin/Resources/config/shop_routing.yml"
  prefix: /{_locale}
  requirements:
    _locale: ^[a-z]{2}(?:_[A-Z]{2})?$
